// Enable prerendering for this page for optimal performance
export const prerender = true;

// Enable client-side routing
export const ssr = true;

// Enable automatic static optimization
export const csr = true;
